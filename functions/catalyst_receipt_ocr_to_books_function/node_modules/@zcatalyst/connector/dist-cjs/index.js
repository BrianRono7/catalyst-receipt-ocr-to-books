"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Connector = exports.Connection = void 0;
const transport_1 = require("@zcatalyst/transport");
const utils_1 = require("@zcatalyst/utils");
const package_json_1 = __importDefault(require("../package.json"));
const { version } = package_json_1.default;
const connection_1 = require("./connection");
Object.defineProperty(exports, "Connector", { enumerable: true, get: function () { return connection_1.Connector; } });
const error_1 = require("./utils/error");
const validators_1 = require("./utils/validators");
const { CLIENT_ID, CLIENT_SECRET, AUTH_URL, REFRESH_URL, CONNECTOR_NAME, COMPONENT } = utils_1.CONSTANTS;
class Connection {
    constructor(propJson, app) {
        this.app = app;
        this.requester = new transport_1.Handler(app, this);
        this.connectionJson = (0, validators_1.getConnectorJson)(propJson);
    }
    getComponentName() {
        return COMPONENT.connector;
    }
    getComponentVersion() {
        return version;
    }
    getConnector(connectorName) {
        if (this.connectionJson === null) {
            throw new error_1.CatalystConnectorError('invalid_input', 'The input passed to connector must be a valid JSON object or a string path to a JSON file', this.connectionJson);
        }
        const connector = this.connectionJson[connectorName];
        (0, utils_1.isNonNullObject)(connector, 'connector.' + connectorName, true);
        (0, utils_1.ObjectHasProperties)(connector, [CLIENT_ID, CLIENT_SECRET, AUTH_URL, REFRESH_URL], 'connector.' + connectorName, true);
        return new connection_1.Connector(this, Object.assign({ [CONNECTOR_NAME]: connectorName }, connector));
    }
}
exports.Connection = Connection;
