"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var _Connector_instances, _Connector_validateOAuthUrl, _Connector_isEncrypted, _Connector_deriveKey, _Connector_encrypt, _Connector_decrypt;
Object.defineProperty(exports, "__esModule", { value: true });
exports.Connector = void 0;
const cache_1 = require("@zcatalyst/cache");
const utils_1 = require("@zcatalyst/utils");
const crypto_1 = __importDefault(require("crypto"));
const error_1 = require("./utils/error");
const { CONNECTOR_NAME, AUTH_URL, REFRESH_URL, ACCESS_TOKEN, REFRESH_TOKEN, CLIENT_ID, CLIENT_SECRET, EXPIRES_IN, REFRESH_IN, REDIRECT_URL, GRANT_TYPE, CODE, REQ_METHOD } = utils_1.CONSTANTS;
const SECRET_KEY = 'secret_key';
class Connector {
    constructor(connectionInstance, connectorDetails) {
        _Connector_instances.add(this);
        this.connectorName = connectorDetails[CONNECTOR_NAME];
        this.authUrl = connectorDetails[AUTH_URL];
        this.refreshUrl = connectorDetails[REFRESH_URL];
        this.refreshToken = connectorDetails[REFRESH_TOKEN];
        this.clientId = connectorDetails[CLIENT_ID];
        this.clientSecret = connectorDetails[CLIENT_SECRET];
        this.expiresIn = parseInt(connectorDetails[EXPIRES_IN]);
        this.refreshIn = parseInt(connectorDetails[REFRESH_IN]) * 1000;
        this.redirectUrl = connectorDetails[REDIRECT_URL];
        this.secretKey = connectorDetails[SECRET_KEY];
        this.accessToken = null;
        this.expiresAt = null;
        this.app = connectionInstance.app;
        this.requester = connectionInstance.requester;
    }
    get _connectorName() {
        return 'ZC_CONN_' + this.connectorName;
    }
    async getAccessToken() {
        if (this.accessToken && this.expiresAt && this.expiresAt > Date.now()) {
            return this.accessToken;
        }
        const cachedTokenObj = await new cache_1.Cache(this.app)
            .segment()
            .get(this._connectorName);
        try {
            const value = JSON.parse(cachedTokenObj.cache_value);
            if (!value?.access_token) {
                return await this.refreshAndPersistToken();
            }
            const expiryTime = value.expires_at;
            if (expiryTime < Date.now()) {
                return await this.refreshAndPersistToken();
            }
            this.expiresAt = expiryTime;
            if (__classPrivateFieldGet(this, _Connector_instances, "m", _Connector_isEncrypted).call(this, value.access_token)) {
                if (!this.secretKey) {
                    throw new error_1.CatalystConnectorError('SECRET_KEY_MISSING', 'The cached access token is encrypted. Please provide a valid secret key to decrypt it.');
                }
                try {
                    this.accessToken = __classPrivateFieldGet(this, _Connector_instances, "m", _Connector_decrypt).call(this, value.access_token, this.secretKey);
                }
                catch {
                    return await this.refreshAndPersistToken();
                }
            }
            else {
                this.accessToken = value.access_token;
            }
            return this.accessToken;
        }
        catch (err) {
            if (err instanceof SyntaxError)
                return await this.refreshAndPersistToken();
            throw err;
        }
    }
    async generateAccessToken(code) {
        await (0, utils_1.wrapValidatorsWithPromise)(() => {
            (0, utils_1.isNonEmptyString)(code, 'grant_token', true);
            (0, utils_1.isNonEmptyString)(this.redirectUrl, REDIRECT_URL, true);
        }, error_1.CatalystConnectorError);
        __classPrivateFieldGet(this, _Connector_instances, "m", _Connector_validateOAuthUrl).call(this, this.authUrl, AUTH_URL);
        const request = {
            method: REQ_METHOD.post,
            url: this.authUrl,
            data: {
                [GRANT_TYPE]: 'authorization_code',
                [CODE]: code,
                [CLIENT_ID]: this.clientId,
                [CLIENT_SECRET]: this.clientSecret,
                [REDIRECT_URL]: this.redirectUrl
            },
            service: "external",
            auth: false
        };
        const resp = await this.requester.send(request);
        const tokenObj = resp.data;
        await (0, utils_1.wrapValidatorsWithPromise)(() => {
            (0, utils_1.isNonNullObject)(tokenObj, 'auth_response', true);
            (0, utils_1.ObjectHasProperties)(tokenObj, [ACCESS_TOKEN, REFRESH_TOKEN, EXPIRES_IN], 'auth_response', true);
        }, error_1.CatalystConnectorError);
        this.accessToken = tokenObj[ACCESS_TOKEN];
        this.refreshToken = tokenObj[REFRESH_TOKEN];
        this.expiresIn = parseInt(tokenObj[EXPIRES_IN]);
        const expires = Date.now() + (this.expiresIn * 1000 - 900000);
        this.expiresAt = this.refreshIn ? Date.now() + this.refreshIn : expires;
        await this.putAccessTokenInCache();
        return this.accessToken;
    }
    async refreshAndPersistToken() {
        await this.refreshAccessToken();
        await this.putAccessTokenInCache();
        return this.accessToken;
    }
    async refreshAccessToken() {
        await (0, utils_1.wrapValidatorsWithPromise)(() => {
            (0, utils_1.isNonEmptyString)(this.refreshToken, 'refresh_token', true);
            (0, utils_1.isNonEmptyString)(this.refreshUrl, 'refresh_url', true);
        }, error_1.CatalystConnectorError);
        __classPrivateFieldGet(this, _Connector_instances, "m", _Connector_validateOAuthUrl).call(this, this.refreshUrl, REFRESH_URL);
        const request = {
            method: REQ_METHOD.post,
            url: this.refreshUrl,
            data: {
                [GRANT_TYPE]: 'refresh_token',
                [CLIENT_ID]: this.clientId,
                [CLIENT_SECRET]: this.clientSecret,
                [REFRESH_TOKEN]: this.refreshToken
            },
            service: "external",
            auth: false
        };
        const resp = await this.requester.send(request);
        const tokenObject = resp.data;
        await (0, utils_1.wrapValidatorsWithPromise)(() => {
            (0, utils_1.isNonNullObject)(tokenObject, 'auth_response', true);
            (0, utils_1.ObjectHasProperties)(tokenObject, [ACCESS_TOKEN, EXPIRES_IN], 'auth_response', true);
        }, error_1.CatalystConnectorError);
        this.accessToken = tokenObject[ACCESS_TOKEN];
        this.expiresIn = parseInt(tokenObject[EXPIRES_IN]);
        const expires = Date.now() + (this.expiresIn * 1000 - 900000);
        this.expiresAt = this.refreshIn ? Date.now() + this.refreshIn : expires;
    }
    async putAccessTokenInCache() {
        const tokenObj = {
            access_token: this.accessToken,
            expiry_in_seconds: this.expiresIn,
            expires_at: this.expiresAt
        };
        if (this.secretKey && this.accessToken) {
            tokenObj.access_token = __classPrivateFieldGet(this, _Connector_instances, "m", _Connector_encrypt).call(this, this.accessToken, this.secretKey);
        }
        const tokenStr = JSON.stringify(tokenObj);
        return new cache_1.Cache(this.app)
            .segment()
            .put(this._connectorName, tokenStr, Math.ceil(this.expiresIn / 3600));
    }
}
exports.Connector = Connector;
_Connector_instances = new WeakSet(), _Connector_validateOAuthUrl = function _Connector_validateOAuthUrl(url, fieldName) {
    if (!(0, utils_1.isURL)(url)) {
        throw new error_1.CatalystConnectorError('INVALID_OAUTH_URL', `The ${fieldName} must be a valid, absolute URL.`);
    }
    let parsedUrl;
    try {
        parsedUrl = new URL(url);
    }
    catch {
        throw new error_1.CatalystConnectorError('INVALID_OAUTH_URL', `The ${fieldName} must be a valid, absolute URL.`);
    }
    const isLoopbackHost = parsedUrl.hostname === 'localhost' || parsedUrl.hostname === '127.0.0.1';
    if (parsedUrl.protocol !== 'https:' && !isLoopbackHost) {
        throw new error_1.CatalystConnectorError('INSECURE_OAUTH_URL', `The ${fieldName} must use HTTPS. Plain HTTP is only permitted for local development ` +
            `against loopback addresses (localhost/127.0.0.1).`);
    }
}, _Connector_isEncrypted = function _Connector_isEncrypted(value) {
    try {
        const decoded = Buffer.from(value, 'base64').toString('utf8');
        const parts = decoded.split(':');
        if (parts.length !== 3 && parts.length !== 4)
            return false;
        const [, ivHex, authTagHex] = parts;
        return ivHex.length === 32 && authTagHex.length === 32;
    }
    catch {
        return false;
    }
}, _Connector_deriveKey = function _Connector_deriveKey(userKey, salt) {
    return crypto_1.default.scryptSync(userKey, salt, 32);
}, _Connector_encrypt = function _Connector_encrypt(text, key) {
    const iv = crypto_1.default.randomBytes(16);
    const salt = crypto_1.default.randomBytes(16);
    const derivedKey = __classPrivateFieldGet(this, _Connector_instances, "m", _Connector_deriveKey).call(this, key, salt);
    const cipher = crypto_1.default.createCipheriv('aes-256-gcm', derivedKey, iv);
    let encrypted = cipher.update(text, 'utf8', 'hex');
    encrypted += cipher.final('hex');
    const authTag = cipher.getAuthTag().toString('hex');
    const result = `${encrypted}:${iv.toString('hex')}:${authTag}:${salt.toString('hex')}`;
    return Buffer.from(result).toString('base64');
}, _Connector_decrypt = function _Connector_decrypt(cipherText, key) {
    const decoded = Buffer.from(cipherText, 'base64').toString('utf8');
    const parts = decoded.split(':');
    const [text, ivHex, authTagHex] = parts;
    const iv = Buffer.from(ivHex, 'hex');
    const authTag = Buffer.from(authTagHex, 'hex');
    const derivedKey = parts.length === 4
        ? __classPrivateFieldGet(this, _Connector_instances, "m", _Connector_deriveKey).call(this, key, Buffer.from(parts[3], 'hex'))
        : crypto_1.default.createHash('sha256').update(key).digest();
    const decipher = crypto_1.default.createDecipheriv('aes-256-gcm', derivedKey, iv);
    decipher.setAuthTag(authTag);
    let decrypted = decipher.update(text, 'hex', 'utf8');
    decrypted += decipher.final('utf8');
    return decrypted;
};
