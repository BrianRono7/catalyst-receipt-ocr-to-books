"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.syncProjectSession = exports.STRATUS_SESSION_VERSION_KEY = exports.STRATUS_JWT_EXPIRY_SKEW_MS = exports.STRATUS_JWT_EXPIRY_KEY = exports.STRATUS_JWT_COOKIE = exports.setStratusJwtExpiry = exports.isStratusJwtFresh = exports.getStratusSessionVersion = exports.getStratusJwtExpiry = exports.clearStratusJwt = exports.ConfigStore = exports.Auth_Protocol = void 0;
exports.getCredentials = getCredentials;
exports.setDefaultProjectConfig = setDefaultProjectConfig;
exports.addDefaultAppHeaders = addDefaultAppHeaders;
exports.getToken = getToken;
exports.setToken = setToken;
exports.collectZCRFToken = collectZCRFToken;
const config_store_1 = require("./config-store");
Object.defineProperty(exports, "ConfigStore", { enumerable: true, get: function () { return config_store_1.ConfigStore; } });
const constants_1 = require("./utils/constants");
const enums_1 = require("./utils/enums");
Object.defineProperty(exports, "Auth_Protocol", { enumerable: true, get: function () { return enums_1.Auth_Protocol; } });
const errors_1 = require("./utils/errors");
const functions_1 = require("./utils/functions");
const session_1 = require("./utils/session");
async function getCredentials() {
    try {
        setDefaultProjectConfig();
        const response = await fetch(`/${constants_1.URL_DIVIDER.RESERVED_URL}/sdk/init`, {
            headers: {
                Accept: 'application/json'
            }
        });
        const credentials = await response.json();
        const finalCredentials = credentials.hasOwnProperty('credentialQR')
            ? credentials.credentialQR
            : credentials;
        validateRequiredCredentials(finalCredentials);
        config_store_1.ConfigStore.set(constants_1.STRATUS_DOMAIN, `-${finalCredentials.environment}${finalCredentials.stratus_suffix}`);
        config_store_1.ConfigStore.set(constants_1.API_DOMAIN, finalCredentials.api_domain);
        config_store_1.ConfigStore.set(constants_1.ZAID, finalCredentials.zaid);
        config_store_1.ConfigStore.set(constants_1.PROJECT_ID, finalCredentials.project_id);
        config_store_1.ConfigStore.set(constants_1.IAM_DOMAIN, finalCredentials.auth_domain);
        config_store_1.ConfigStore.set(constants_1.ENVIRONMENT, finalCredentials.environment);
        config_store_1.ConfigStore.set(constants_1.IS_APPSAIL, finalCredentials.is_appsail);
        config_store_1.ConfigStore.set(constants_1.PROJECT_DOMAIN, finalCredentials.project_domain);
        if (finalCredentials.org_id) {
            config_store_1.ConfigStore.set(constants_1.ORG_ID, finalCredentials.org_id);
        }
        config_store_1.ConfigStore.set(constants_1.INITIALIZED, true + '');
        (0, functions_1.setGlobal)('__catalyst', finalCredentials);
        (0, session_1.syncProjectSession)(finalCredentials);
    }
    catch (error) {
        throw new errors_1.CatalystAuthError('CREDENTIAL_FETCH_ERROR', `Failed to fetch credentials: ${error instanceof Error ? error.message : 'Unknown error'}`, 500);
    }
}
function setDefaultProjectConfig() {
    for (const [key, value] of Object.entries(constants_1.defaultConfig)) {
        if (value !== undefined && value !== null) {
            config_store_1.ConfigStore.set(String(key), value);
        }
        else {
            config_store_1.ConfigStore.set(String(key), '');
        }
    }
}
function validateRequiredCredentials(credentialJson) {
    for (const requirement of constants_1.REQUIREMENT.INIT_REQUIRE) {
        if (!credentialJson.hasOwnProperty(requirement)) {
            throw new errors_1.CatalystAuthError('PROPERTY_NOT_FOUND', `Missing required property: ${requirement}`, 400);
        }
    }
}
function addDefaultAppHeaders(headers = {}) {
    const normalizedHeaders = headers;
    const currentAccept = normalizedHeaders['Accept'];
    if (!currentAccept) {
        normalizedHeaders['Accept'] = 'application/vnd.catalyst.v2+json';
    }
    else {
        normalizedHeaders['Accept'] = `application/vnd.catalyst.v2+json, ${currentAccept}`;
    }
    normalizedHeaders['CATALYST-COMPONENT'] = 'true';
    const orgId = config_store_1.ConfigStore.get(constants_1.ORG_ID);
    if (orgId) {
        normalizedHeaders['CATALYST-ORG'] = orgId;
    }
    return normalizedHeaders;
}
function getToken(key) {
    let jwtAuthToken = '';
    const cookies = document.cookie.split(';');
    const cookiesLen = cookies.length;
    for (let i = 0; i < cookiesLen; i++) {
        const keyValuePairSplitted = cookies[i].split('=');
        if (keyValuePairSplitted[0].trim() === (key ? key : 'cookie')) {
            jwtAuthToken = keyValuePairSplitted[1];
        }
    }
    return jwtAuthToken;
}
function setToken(authObj, key) {
    const maxAgeSeconds = authObj.expires_in
        ? Math.max(0, Math.floor((authObj.expires_in - Date.now()) / 1000))
        : 0;
    document.cookie = `${key ? key : 'cookie'}=${authObj.access_token}; max-age=${maxAgeSeconds}; path=/`;
}
async function collectZCRFToken() {
    return new Promise((resolve, reject) => {
        try {
            const cookies = document.cookie.split(';');
            for (const cookie of cookies) {
                const keyVal = cookie.split('=');
                if (keyVal.length === 2 &&
                    keyVal[0].trim() === constants_1.CSRF_TOKEN_KEY &&
                    keyVal[1].trim().length > 0) {
                    config_store_1.ConfigStore.set(constants_1.CSRF_TOKEN, keyVal[1].trim());
                    resolve('success');
                    return;
                }
            }
            resolve('success');
        }
        catch {
            resolve('success');
        }
    });
}
__exportStar(require("./utils/constants"), exports);
var session_2 = require("./utils/session");
Object.defineProperty(exports, "clearStratusJwt", { enumerable: true, get: function () { return session_2.clearStratusJwt; } });
Object.defineProperty(exports, "getStratusJwtExpiry", { enumerable: true, get: function () { return session_2.getStratusJwtExpiry; } });
Object.defineProperty(exports, "getStratusSessionVersion", { enumerable: true, get: function () { return session_2.getStratusSessionVersion; } });
Object.defineProperty(exports, "isStratusJwtFresh", { enumerable: true, get: function () { return session_2.isStratusJwtFresh; } });
Object.defineProperty(exports, "setStratusJwtExpiry", { enumerable: true, get: function () { return session_2.setStratusJwtExpiry; } });
Object.defineProperty(exports, "STRATUS_JWT_COOKIE", { enumerable: true, get: function () { return session_2.STRATUS_JWT_COOKIE; } });
Object.defineProperty(exports, "STRATUS_JWT_EXPIRY_KEY", { enumerable: true, get: function () { return session_2.STRATUS_JWT_EXPIRY_KEY; } });
Object.defineProperty(exports, "STRATUS_JWT_EXPIRY_SKEW_MS", { enumerable: true, get: function () { return session_2.STRATUS_JWT_EXPIRY_SKEW_MS; } });
Object.defineProperty(exports, "STRATUS_SESSION_VERSION_KEY", { enumerable: true, get: function () { return session_2.STRATUS_SESSION_VERSION_KEY; } });
Object.defineProperty(exports, "syncProjectSession", { enumerable: true, get: function () { return session_2.syncProjectSession; } });
