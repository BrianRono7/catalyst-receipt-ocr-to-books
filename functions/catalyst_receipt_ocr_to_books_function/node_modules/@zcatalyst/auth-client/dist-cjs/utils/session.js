"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.STRATUS_JWT_COOKIE = exports.STRATUS_JWT_EXPIRY_SKEW_MS = exports.STRATUS_JWT_EXPIRY_KEY = exports.STRATUS_SESSION_VERSION_KEY = void 0;
exports.clearStratusJwt = clearStratusJwt;
exports.setStratusJwtExpiry = setStratusJwtExpiry;
exports.getStratusJwtExpiry = getStratusJwtExpiry;
exports.isStratusJwtFresh = isStratusJwtFresh;
exports.syncProjectSession = syncProjectSession;
exports.getStratusSessionVersion = getStratusSessionVersion;
const PROJECT_CONTEXT_KEY = '__catalyst_last_project_ctx';
exports.STRATUS_SESSION_VERSION_KEY = '__catalyst_stratus_session_version';
exports.STRATUS_JWT_EXPIRY_KEY = '__catalyst_stratus_jwt_expires_at';
exports.STRATUS_JWT_EXPIRY_SKEW_MS = 30000;
exports.STRATUS_JWT_COOKIE = 'stratus_jwt';
function clearStratusJwt() {
    if (typeof document === 'undefined') {
        return;
    }
    document.cookie = `${exports.STRATUS_JWT_COOKIE}=; path=/; max-age=0; expires=Thu, 01 Jan 1970 00:00:00 GMT`;
    if (typeof window !== 'undefined') {
        try {
            window.sessionStorage?.removeItem(exports.STRATUS_JWT_EXPIRY_KEY);
        }
        catch {
        }
    }
}
function setStratusJwtExpiry(expiresAtMs) {
    if (typeof window === 'undefined' || !Number.isFinite(expiresAtMs)) {
        return;
    }
    safeWrite(window.sessionStorage ?? null, exports.STRATUS_JWT_EXPIRY_KEY, String(expiresAtMs));
}
function getStratusJwtExpiry() {
    if (typeof window === 'undefined') {
        return 0;
    }
    const raw = safeRead(window.sessionStorage ?? null, exports.STRATUS_JWT_EXPIRY_KEY);
    const parsed = raw ? Number(raw) : 0;
    return Number.isFinite(parsed) ? parsed : 0;
}
function isStratusJwtFresh() {
    if (!hasStratusJwtCookie()) {
        return false;
    }
    const expiry = getStratusJwtExpiry();
    if (expiry === 0) {
        return true;
    }
    return expiry - exports.STRATUS_JWT_EXPIRY_SKEW_MS > Date.now();
}
function hasStratusJwtCookie() {
    if (typeof document === 'undefined') {
        return false;
    }
    return document.cookie.split(';').some((c) => c.trim().startsWith(`${exports.STRATUS_JWT_COOKIE}=`));
}
function buildContextKey(creds) {
    if (!creds) {
        return '';
    }
    return [
        creds.project_id ?? '',
        creds.zaid ?? '',
        creds.project_domain ?? '',
        creds.environment ?? ''
    ].join('|');
}
function safeRead(storage, key) {
    try {
        return storage ? storage.getItem(key) : null;
    }
    catch {
        return null;
    }
}
function safeWrite(storage, key, value) {
    try {
        storage && storage.setItem(key, value);
    }
    catch {
    }
}
function bumpStratusSessionVersion() {
    if (typeof window === 'undefined') {
        return;
    }
    const next = Date.now().toString();
    safeWrite(window.sessionStorage ?? null, exports.STRATUS_SESSION_VERSION_KEY, next);
}
function syncProjectSession(credentials) {
    if (typeof window === 'undefined') {
        return false;
    }
    const next = buildContextKey(credentials);
    if (!next) {
        return false;
    }
    const ls = window.localStorage ?? null;
    const previous = safeRead(ls, PROJECT_CONTEXT_KEY);
    let switched = false;
    if (previous === null) {
        if (hasStratusJwtCookie()) {
            clearStratusJwt();
            switched = true;
        }
    }
    else if (previous !== next) {
        clearStratusJwt();
        switched = true;
    }
    safeWrite(ls, PROJECT_CONTEXT_KEY, next);
    if (switched) {
        bumpStratusSessionVersion();
    }
    return switched;
}
function getStratusSessionVersion() {
    if (typeof window === 'undefined') {
        return '';
    }
    return safeRead(window.sessionStorage ?? null, exports.STRATUS_SESSION_VERSION_KEY) ?? '';
}
